name: mcq
kind: eval
description: Multiple-choice accuracy suite using exact lm-eval-harness 0.4.11 task IDs.
selection:
  enabled_only: true
eval:
  backend: lm_eval_harness
  model_backend: vllm
  version: 0.4.11
  dataset: lm_eval_harness
  dtype: auto
  metric: acc_norm
  metric_fallbacks:
    - acc
  tracked_metrics:
    - acc
    - acc_norm
  metric_aggregation: macro_mean
  batch_size: auto
  num_fewshot: 0
  tasks:
    - boolq
    - arc_easy
    - arc_challenge
    - winogrande
    - piqa
    - hellaswag
    - openbookqa
report:
  result_dir: results/leaderboard/eval
  output_dir: results/tables
  filename: mcq.md
