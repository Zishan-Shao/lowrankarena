# Legacy Experimental Kernels

This folder holds old kernel experiments that are still worth keeping, but are
only used by legacy benchmark scripts. They are not part of the production
runtime and do not belong in the versioned archive.

Current layout:

- `geglu/`
  legacy GEGLU / encoder-side kernel experiments used only by archive-era
  microbench scripts
