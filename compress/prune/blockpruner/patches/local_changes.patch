diff --git a/block_search.py b/block_search.py
index 62f2540..ed7ca68 100644
--- a/block_search.py
+++ b/block_search.py
@@ -109,7 +109,7 @@ class MaskedLlamaDecoderLayer(nn.Module):
             hidden_states = self.input_layernorm(hidden_states)
 
             # Self Attention
-            hidden_states, self_attn_weights, present_key_value = self.self_attn(
+            attn_outputs = self.self_attn(
                 hidden_states=hidden_states,
                 attention_mask=attention_mask,
                 position_ids=position_ids,
@@ -118,6 +118,15 @@ class MaskedLlamaDecoderLayer(nn.Module):
                 use_cache=use_cache,
                 **kwargs,
             )
+            hidden_states = attn_outputs[0]
+            self_attn_weights = None
+            present_key_value = None
+            if output_attentions and len(attn_outputs) >= 2:
+                self_attn_weights = attn_outputs[1]
+            if use_cache:
+                cache_idx = 2 if output_attentions else 1
+                if len(attn_outputs) > cache_idx:
+                    present_key_value = attn_outputs[cache_idx]
             hidden_states = residual.to(hidden_states.device) + hidden_states
         else:
             self_attn_weights = None
diff --git a/eval.py b/eval.py
index 125e5c3..715e18d 100644
--- a/eval.py
+++ b/eval.py
@@ -2,11 +2,23 @@ from typing import Optional, Tuple
 import argparse
 import json
 import logging
+import os
+import sys
 import torch
 import torch.nn as nn
+import datasets
 from transformers import AutoModelForCausalLM, AutoTokenizer
 from transformers import set_seed; set_seed(42)
 import utils
+
+LOCAL_LM_EVAL_ROOT = os.path.join(os.path.dirname(__file__), "lm_eval")
+if LOCAL_LM_EVAL_ROOT not in sys.path:
+    sys.path.insert(0, LOCAL_LM_EVAL_ROOT)
+
+if not hasattr(datasets, "load_metric"):
+    from evaluate import load as _load_metric
+    datasets.load_metric = _load_metric
+
 import lm_eval
 from lm_eval import utils as lm_eval_utils
 from lm_eval.api.registry import ALL_TASKS
@@ -44,6 +56,8 @@ def parse_args() -> argparse.Namespace:
         type=str,
         help="...",
     )
+    parser.add_argument("--output-json", type=str, default=None, help="Optional path to save eval metrics as JSON.")
+    parser.add_argument("--label", type=str, default=None, help="Optional run label stored in the output JSON.")
     parser.add_argument(
         "--del-block-num",
         type=int,
@@ -68,6 +82,7 @@ def parse_args() -> argparse.Namespace:
         default=["piqa", "winogrande", "hellaswag", "arc_easy", "arc_challenge"],
     )
     parser.add_argument('--num-fewshot', type=int, default=0, help="Number of fewshots for all tasks.")
+    parser.add_argument('--limit', type=int, default=None, help="Optional sample limit for smoke evaluation.")
     return parser.parse_args()
 
 
@@ -110,7 +125,7 @@ class MaskedLlamaDecoderLayer(nn.Module):
             hidden_states = self.input_layernorm(hidden_states)
 
             # Self Attention
-            hidden_states, self_attn_weights, present_key_value = self.self_attn(
+            attn_outputs = self.self_attn(
                 hidden_states=hidden_states,
                 attention_mask=attention_mask,
                 position_ids=position_ids,
@@ -119,6 +134,15 @@ class MaskedLlamaDecoderLayer(nn.Module):
                 use_cache=use_cache,
                 **kwargs,
             )
+            hidden_states = attn_outputs[0]
+            self_attn_weights = None
+            present_key_value = None
+            if output_attentions and len(attn_outputs) >= 2:
+                self_attn_weights = attn_outputs[1]
+            if use_cache:
+                cache_idx = 2 if output_attentions else 1
+                if len(attn_outputs) > cache_idx:
+                    present_key_value = attn_outputs[cache_idx]
             hidden_states = residual.to(hidden_states.device) + hidden_states
         else:
             self_attn_weights = None
@@ -167,6 +191,7 @@ def remove_redundant_blocks(args, model):
     del_block_list = json.load(open(args.ppl_search_file, "r"))[str(args.del_block_num)]
     logging.info(f"chosen del_block_list: {del_block_list}")
     apply_block_masks(model, del_block_list)
+    return del_block_list
 
 
 def eval(args, model, tokenizer):
@@ -176,7 +201,13 @@ def eval(args, model, tokenizer):
 
     logging.info(f"Selected Tasks: {task_names}")
 
-    results = lm_eval.simple_evaluate(hflm, tasks=task_names, num_fewshot=args.num_fewshot, batch_size=args.batch_size)['results']
+    results = lm_eval.simple_evaluate(
+        hflm,
+        tasks=task_names,
+        num_fewshot=args.num_fewshot,
+        batch_size=args.batch_size,
+        limit=args.limit,
+    )['results']
 
     metric_vals = {task: round(result.get('acc_norm,none', result['acc,none']), 4) for task, result in results.items()}
     logging.info(json.dumps(metric_vals, indent=4))
@@ -188,6 +219,12 @@ def eval(args, model, tokenizer):
 
     acc_avg = calculate_avg_accuracy(task_names, results)
     logging.info(f"Average accuracy across tasks: {acc_avg}")
+    return {
+        "selected_tasks": task_names,
+        "results": results,
+        "average_accuracy": acc_avg,
+        "report_metric_by_task": metric_vals,
+    }
 
 
 def main() -> None:
@@ -221,15 +258,44 @@ def main() -> None:
         batch_size=args.ppl_eval_batch_size
     )
 
-    remove_redundant_blocks(args, model)
-    logging.info(f"pruned model size: {get_model_params(model)/1e9:.3f}B")
-    logging.info(f"pruning ratio: {(1- get_model_params(model)/model_size) * 100:.2f}")
+    del_block_list = remove_redundant_blocks(args, model)
+    pruned_model_size = get_model_params(model)
+    pruning_ratio = (1 - pruned_model_size / model_size) * 100
+    logging.info(f"pruned model size: {pruned_model_size/1e9:.3f}B")
+    logging.info(f"pruning ratio: {pruning_ratio * 1.0:.2f}")
 
     dataset_ppl = utils.evaluate_ppl(model, model.config.pad_token_id, test_loader)
     logging.info(f'model ppl: {dataset_ppl:.4f}')
 
+    eval_payload = None
     if args.do_eval:
-        eval(args, model, tokenizer)
+        eval_payload = eval(args, model, tokenizer)
+
+    if args.output_json:
+        payload = {
+            "label": args.label,
+            "model_path": args.model_path,
+            "ppl_search_file": args.ppl_search_file,
+            "del_block_num": args.del_block_num,
+            "del_block_list": del_block_list,
+            "cal_dataset": args.cal_dataset,
+            "ppl_eval_seqlen": args.ppl_eval_seqlen,
+            "ppl_eval_batch_size": args.ppl_eval_batch_size,
+            "batch_size": args.batch_size,
+            "compute_dtype": args.compute_dtype,
+            "original_model_params": model_size,
+            "pruned_model_params": pruned_model_size,
+            "pruning_ratio_percent": pruning_ratio,
+            "ppl": dataset_ppl,
+            "eval": eval_payload,
+        }
+        output_path = args.output_json
+        output_dir = os.path.dirname(output_path)
+        if output_dir:
+            os.makedirs(output_dir, exist_ok=True)
+        with open(output_path, "w") as f:
+            json.dump(payload, f, indent=2)
+            f.write("\n")
 
 if __name__ == "__main__":
     main()
diff --git a/utils.py b/utils.py
index 098a758..9da3038 100644
--- a/utils.py
+++ b/utils.py
@@ -1,14 +1,15 @@
 # Modified from https://github.com/microsoft/TransformerCompression
 # Copyright (c) Microsoft Corporation.
 # Licensed under the MIT license.
-import datetime
-import time
-import gc
-import inspect
-import logging
-import pathlib
-from typing import TypeVar
-import datasets
+import datetime
+import time
+import gc
+import inspect
+import logging
+import pathlib
+from typing import TypeVar
+import os
+import datasets
 import torch
 from torch.utils.data import DataLoader, Dataset, SubsetRandomSampler
 from transformers import PreTrainedTokenizerBase
@@ -163,8 +164,8 @@ def get_dataset(name: str) -> datasets.DatasetDict:
     """
     logging.info(f"Loading dataset: {name}")
 
-    ds_properties = {
-        "wikitext2": {"path": "/data/lgzhong/tiny/train/svd/data/wikitext", "config_name": "wikitext-2-raw-v1"},
+    ds_properties = {
+        "wikitext2": {"path": "/data/lgzhong/tiny/train/svd/data/wikitext", "config_name": "wikitext-2-raw-v1"},
         "ptb": {"path": "ptb_text_only", "config_name": "penn_treebank"},
         "c4": {
             "path": "allenai/c4",
@@ -181,10 +182,12 @@ def get_dataset(name: str) -> datasets.DatasetDict:
     if name not in ds_properties:
         raise NotImplementedError("The provided dataset is not supported")
 
-    properties = ds_properties[name]
-    ds = datasets.load_dataset(
-        properties["path"], name=properties.get("config_name"), data_files=properties.get("data_files")
-    )
+    properties = ds_properties[name]
+    if name == "wikitext2" and not os.path.exists(properties["path"]):
+        properties = {"path": "wikitext", "config_name": "wikitext-2-raw-v1"}
+    ds = datasets.load_dataset(
+        properties["path"], name=properties.get("config_name"), data_files=properties.get("data_files")
+    )
 
     if "cols_to_remove" in properties:
         ds = ds.remove_columns(properties["cols_to_remove"])
@@ -219,13 +222,18 @@ def prepare_test_dataloader(
 
     logging.info(f"Preparing test dataloader")
 
-    class TestDataset(Dataset):
-        def __init__(self, ds, tokenizer, seqlen=2048):
-            """Tokenize the entire dataset and reshape it into sequences of length seqlen."""
-
-            tokenized_ds = tokenizer("\n\n".join(ds['text']), return_tensors='pt')
-            nsamples = tokenized_ds.input_ids.numel() // seqlen
-            input_ids = tokenized_ds.input_ids[0, : nsamples * seqlen]
+    class TestDataset(Dataset):
+        def __init__(self, ds, tokenizer, seqlen=2048):
+            """Tokenize the entire dataset and reshape it into sequences of length seqlen."""
+
+            tokenized_ds = tokenizer("\n\n".join(ds['text']), return_tensors='pt')
+            nsamples = tokenized_ds.input_ids.numel() // seqlen
+            if nsamples == 0:
+                raise ValueError(
+                    f"Tokenized test dataset is shorter than seqlen={seqlen}. "
+                    "Increase cal-nsamples or reduce ppl-eval-seqlen."
+                )
+            input_ids = tokenized_ds.input_ids[0, : nsamples * seqlen]
             input_ids = input_ids.reshape(nsamples, seqlen)
             attn_mask = tokenized_ds.attention_mask[0, : nsamples * seqlen]
             attn_mask = attn_mask.reshape(nsamples, seqlen)
