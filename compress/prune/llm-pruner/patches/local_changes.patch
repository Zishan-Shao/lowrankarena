diff --git a/hf_prune.py b/hf_prune.py
index c6eb2f0..e00f592 100644
--- a/hf_prune.py
+++ b/hf_prune.py
@@ -11,8 +11,14 @@ from typing import Tuple
 import torch
 import numpy as np
 from transformers import LlamaTokenizer, GenerationConfig, LlamaConfig
+from transformers.generation.utils import GenerationMixin
 from LLMPruner.models.hf_llama.modeling_llama import LlamaForCausalLM, LlamaRMSNorm, LlamaAttention, LlamaMLP
 
+
+class LlamaForCausalLMWithGen(LlamaForCausalLM, GenerationMixin):
+    """Named class so the model remains picklable when generate is needed."""
+    pass
+
 import LLMPruner.torch_pruning as tp 
 from LLMPruner.pruner import hf_llama_pruner as llama_pruner
 from LLMPruner.utils.logger import LoggerWithDepth
@@ -41,6 +47,12 @@ def main(args):
         args.base_model,
         low_cpu_mem_usage=True if args.torch_version >=1.9 else False
     )
+    # LLMPruner's custom LlamaForCausalLM may miss GenerationMixin in newer transformers.
+    # Attach generate for evaluation prompts without changing pruning logic.
+    if not hasattr(model, "generate"):
+        model.__class__ = LlamaForCausalLMWithGen
+    if getattr(model, "generation_config", None) is None:
+        model.generation_config = GenerationConfig.from_model_config(model.config)
     if args.device != "cpu":
         model.half()
     model.to(args.device)
diff --git a/llama3.py b/llama3.py
index df726ed..227fddf 100644
--- a/llama3.py
+++ b/llama3.py
@@ -94,6 +94,15 @@ def main(args):
         raise NotImplementedError
 
     logger.log("Use {} pruner...".format(pruner_type))
+
+    # torch_pruning needs a tensor output to build the dependency graph.
+    # LlamaForCausalLM can return a tuple or a ModelOutput; unwrap to logits.
+    def _tp_output_transform(out):
+        if isinstance(out, (tuple, list)):
+            return out[0]
+        if hasattr(out, "logits"):
+            return out.logits
+        return out
     
     if args.block_wise:
         kwargs = {
@@ -102,6 +111,8 @@ def main(args):
             "iterative_steps": args.iterative_steps,
             "ch_sparsity": args.pruning_ratio, 
             "ignored_layers":[],
+            "output_transform": _tp_output_transform,
+            "output_transform": _tp_output_transform,
             "channel_groups": {
             },
             "consecutive_groups": {
diff --git a/lm-evaluation-harness/lm_eval/models/huggingface.py b/lm-evaluation-harness/lm_eval/models/huggingface.py
index a96bdeb..8415c48 100644
--- a/lm-evaluation-harness/lm_eval/models/huggingface.py
+++ b/lm-evaluation-harness/lm_eval/models/huggingface.py
@@ -71,6 +71,7 @@ class HuggingFaceAutoLM(BaseLM):
         self,
         pretrained: Optional[str] = None,
         checkpoint: Optional[str] = None,
+        state_dict_checkpoint: Optional[str] = None,
         config_pretrained:Optional[str] = None,
         tokenizer: Optional[str] = None,
         subfolder: Optional[str] = None,
@@ -145,7 +146,11 @@ class HuggingFaceAutoLM(BaseLM):
         """
         super().__init__()
 
-        assert isinstance(pretrained, str) or (isinstance(checkpoint, str) and isinstance(config_pretrained, str))
+        assert (
+            isinstance(pretrained, str)
+            or (isinstance(checkpoint, str) and isinstance(config_pretrained, str))
+            or (isinstance(state_dict_checkpoint, str) and isinstance(config_pretrained, str))
+        )
         assert isinstance(device, str)
         assert isinstance(batch_size, (int, str))
         if (
@@ -200,6 +205,34 @@ class HuggingFaceAutoLM(BaseLM):
             self.model = pruned_dict['model']
             print("Load from Pruned Model: {}".format(checkpoint))
             self.model.half()
+        elif state_dict_checkpoint is not None:
+            self.tokenizer = self._create_auto_tokenizer(
+                pretrained=config_pretrained,
+                revision=revision,
+                subfolder=subfolder,
+                tokenizer=tokenizer,
+            )
+            self.model = self._create_auto_model(
+                pretrained=config_pretrained,
+                trust_remote_code=trust_remote_code,
+                revision=revision,
+                subfolder=subfolder,
+                torch_dtype=_get_dtype(dtype, self._config),
+                **model_kwargs,
+            )
+            state_dict = torch.load(state_dict_checkpoint, map_location='cpu')
+            if (
+                isinstance(state_dict, dict)
+                and "state_dict" in state_dict
+                and isinstance(state_dict["state_dict"], dict)
+            ):
+                state_dict = state_dict["state_dict"]
+            incompatible = self.model.load_state_dict(state_dict, strict=False)
+            print("Load from state_dict checkpoint: {}".format(state_dict_checkpoint))
+            if incompatible.missing_keys or incompatible.unexpected_keys:
+                print("Missing keys: {}".format(list(incompatible.missing_keys)))
+                print("Unexpected keys: {}".format(list(incompatible.unexpected_keys)))
+            self.model.half()
         else:
             #from transformers import LlamaTokenizer
             #self.tokenizer = LlamaTokenizer.from_pretrained(pretrained)
